---
phase: 04-long-list-final-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
autonomous: true
requirements:
  - XCUT-07

must_haves:
  truths:
    - "All 7 .txt wordlist files exist and pass tidy -AAAA with 'Uniquely decodable? : true'"
    - "All 7 .txt wordlist files match their target word counts exactly"
    - "All 7 .txt wordlist files meet or exceed their target entropy (bits/word)"
    - "All 8 SQLite .db files exist and pass PRAGMA integrity_check with result 'ok'"
    - "All 8 SQLite .db files contain the correct number of rows matching their corresponding .txt files"
    - "README.md lists all 7 wordlist files with accurate word counts and entropy values"
    - "README.md long list section updated with actual Malay list statistics (not placeholder English stats)"
  artifacts:
    - path: "lists/jalan-dewan-bahasa-besar.txt"
      provides: "17,576 uniquely decodable Malay words (~14.1 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-sederhana.txt"
      provides: "8,192 uniquely decodable Malay words (13.0 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-dadu.txt"
      provides: "7,776 dice-formatted Malay words (12.925 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-dadu-bersih.txt"
      provides: "7,776 clean Malay words (12.925 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-kecil-alpha.txt"
      provides: "1,296 uniquely decodable Malay words (10.34 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-kecil-alpha-dadu.txt"
      provides: "1,296 dice-formatted Malay words (10.34 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-kecil-qwerty.txt"
      provides: "1,296 uniquely decodable Malay words (10.34 bits/word)"
      contains: "Uniquely decodable? : true"
    - path: "lists/jalan-dewan-bahasa-kecil-qwerty-dadu.txt"
      provides: "1,296 dice-formatted Malay words (10.34 bits/word)"
      contains: "Uniquely decodable? : true"
  key_links:
    - from: "README.md §Senarai Besar"
      to: "lists/jalan-dewan-bahasa-besar.txt"
      via: "tidy -AAAA output statistics"
      pattern: "walk-jalan-dewan-bahasa-besar.txt"
    - from: "README.md"
      to: "db/ directory"
      via: "SQLite PRAGMA integrity_check + row counts"
      pattern: "db/"
---

<objective>
Cross-verify ALL 7 output .txt wordlist files and ALL 8 SQLite databases for unique decodability, word counts, entropy, and database integrity — then update README.md with the final file listing, accurate word counts, and entropy values for all produced lists.

**Purpose:** This is the final quality gate for the entire project. Every output file must pass verification before the project is complete. The README must accurately reflect the actual deliverables — the long list section currently has placeholder statistics from the English original that must be replaced with verified Malay list stats.

**Output:** Updated README.md with accurate file listing, word counts, and entropy values. No new data files — verification only.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-medium-diceware-lists/03-03-SUMMARY.md
@.planning/phases/02-short-lists-pipeline-validation/02-03-SUMMARY.md
@README.md

<interfaces>
<!-- Key contracts the executor needs. -->

**Target word counts and entropy (from REQUIREMENTS.md + ROADMAP.md):**

| File | Target Words | Target Entropy (bits) | Dice Variant? |
|------|-------------|----------------------|---------------|
| `lists/jalan-dewan-bahasa-besar.txt` | 17,576 | ≥ 14.101 | No |
| `lists/jalan-dewan-bahasa-sederhana.txt` | 8,192 | ≥ 13.000 | No |
| `lists/jalan-dewan-bahasa-dadu.txt` | 7,776 | ≥ 12.925 | Yes (5-digit) |
| `lists/jalan-dewan-bahasa-dadu-bersih.txt` | 7,776 | ≥ 12.925 | Clean variant |
| `lists/jalan-dewan-bahasa-kecil-alpha.txt` | 1,296 | ≥ 10.340 | No |
| `lists/jalan-dewan-bahasa-kecil-alpha-dadu.txt` | 1,296 | ≥ 10.340 | Yes (4-digit) |
| `lists/jalan-dewan-bahasa-kecil-qwerty.txt` | 1,296 | ≥ 10.340 | No |
| `lists/jalan-dewan-bahasa-kecil-qwerty-dadu.txt` | 1,296 | ≥ 10.340 | Yes (4-digit) |

Wait — there are only 7 .txt files (not 8). The above lists 8 entries but `besar` isn't yet created (it's the Plan 04-01 output). The 7 existing files + besar = 8 total .txt files? No, let me recount:

Actually existing at start of Phase 4:
1. `kecil-alpha.txt` — 1,296
2. `kecil-alpha-dadu.txt` — 1,296
3. `kecil-qwerty.txt` — 1,296
4. `kecil-qwerty-dadu.txt` — 1,296
5. `sederhana.txt` — 8,192
6. `dadu.txt` — 7,776
7. `dadu-bersih.txt` — 7,776

After Phase 4:
8. `besar.txt` — 17,576

So total: 7 existing + 1 new = 8 .txt files. But ROADMAP says "8 output files (7 .txt + 8 .db)" — that seems like a typo. Let me check again...

ROADMAP Phase 4 success criteria: "All 8 output files (7 .txt + 8 .db) pass verification" — this means 7 existing .txt files + the new besar.txt = 8 total? No, that doesn't add up either.

Looking at ROADMAP again: actually the success criteria count all final outputs. The existing 7 .txt files (from Phase 1-3) + 1 new = 8 .txt files total. Plus 8 .db files (7 existing + 1 new). But the text says "7 .txt" which is confusing. Let me just verify all existing and new files regardless.

OK, after re-reading: "All 8 output files (7 .txt + 8 .db)" — this seems like it should be "8 .txt + 8 .db" since there are 7 existing .txt + 1 new = 8. I'll plan for all 8 .txt + 8 .db.

**tidy -AAAA verification command** (for each .txt file):
```bash
tidy -AAAA -z nfc --locale ms lists/jalan-dewan-bahasa-{name}.txt
```
Expected output includes: `List length: {N}`, `Uniquely decodable? : true`, `Entropy per word: {bits}`, `Above brute force line? : true`

**SQLite verification commands** (for each .db file):
```bash
sqlite3 db/{name}.db "SELECT COUNT(*) FROM words;"
sqlite3 db/{name}.db "PRAGMA integrity_check;"
```

**Current README structure** (lines 1-169):
- Line 12-35: Senarai Besar — has PLACEHOLDER stats from English original (Needs update with actual Malay stats)
- Line 37-62: Senarai Sederhana — has actual stats (Phase 3 output, may need verification)
- Line 64-91: Senarai Dadu — has actual stats (Phase 3 output, may need verification)
- Line 93-143: Senarai Kecil (Alpha + QWERTY) — has actual stats (Phase 2 output, may need verification)
- Line 147-168: SQLite section + licensing — generally fine

The critical update: the "Senarai Besar" section (lines 12-35) currently shows placeholder stats from the English `orchard-street-long.txt`. After Phase 4, this section must reflect the actual `jalan-dewan-bahasa-besar.txt` stats from `tidy -AAAA` output.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: cross-verify all 8 .txt wordlist files and 8 SQLite databases</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (XCUT-07 — entropy and word count verification targets)
    - .planning/ROADMAP.md (Phase 4 success criteria, word count targets)
    - lists/jalan-dewan-bahasa-kecil-alpha.txt
    - lists/jalan-dewan-bahasa-kecil-alpha-dadu.txt
    - lists/jalan-dewan-bahasa-kecil-qwerty.txt
    - lists/jalan-dewan-bahasa-kecil-qwerty-dadu.txt
    - lists/jalan-dewan-bahasa-sederhana.txt
    - lists/jalan-dewan-bahasa-dadu.txt
    - lists/jalan-dewan-bahasa-dadu-bersih.txt
    - lists/jalan-dewan-bahasa-besar.txt (Plan 04-01 output — MUST exist before this task)
  </read_first>
  <action>
Run comprehensive verification on ALL output files. This task confirms every deliverable meets its target — the final quality gate for the project.

**Part A: .txt Wordlist Verification (XCUT-07 — entropy and word count)**

For EACH of the 8 .txt wordlist files, run `tidy -AAAA` and verify:

```bash
tidy -AAAA -z nfc --locale ms lists/jalan-dewan-bahasa-{name}.txt
```

Extract and verify these attributes from the `tidy -AAAA` output:

| File | Expected Words | Expected Entropy ≥ | Dice Roll Range |
|------|---------------|-------------------|-----------------|
| `besar.txt` | 17,576 | 14.101 bits | N/A |
| `sederhana.txt` | 8,192 | 13.000 bits | N/A |
| `dadu.txt` | 7,776 | 12.925 bits | 11111–66666 (5-digit) |
| `dadu-bersih.txt` | 7,776 | 12.925 bits | N/A |
| `kecil-alpha.txt` | 1,296 | 10.340 bits | N/A |
| `kecil-alpha-dadu.txt` | 1,296 | 10.340 bits | 1111–6666 (4-digit) |
| `kecil-qwerty.txt` | 1,296 | 10.340 bits | N/A |
| `kecil-qwerty-dadu.txt` | 1,296 | 10.340 bits | 1111–6666 (4-digit) |

For each file, confirm:
1. **`List length:`** matches target word count exactly
2. **`Uniquely decodable? : true`** — non-negotiable
3. **`Entropy per word:`** ≥ target entropy (log₂ of word count)
4. **`Above brute force line? : true`**
5. **`Free of prefix words? : false`** — expected for Schlinkert-pruned lists (except dadu-bersih may be true)

Additional checks:
- **Dice variant format:** For dadu.txt, alpha-dadu.txt, qwerty-dadu.txt — verify format `^\d+\t[a-z]+$` using `grep -cPv '^\d+\t[a-z]+$'` — expected: 0 non-conforming lines
- **Dadu dice roll range:** Verify rolls cover 11111–66666 for dadu.txt (7,776 = 6^5 exactly)
- **Short dice roll range:** Verify rolls cover 1111–6666 for alpha-dadu and qwerty-dadu (1,296 = 6^4 exactly)
- **Dadu bersih vs dadu word match:** `diff <(cut -f2 lists/jalan-dewan-bahasa-dadu.txt | sort) <(sort lists/jalan-dewan-bahasa-dadu-bersih.txt)` — expected: no differences
- **Lowercase a-z only:** `grep -cP '[^a-z]' lists/jalan-dewan-bahasa-besar.txt` — expected: 0 (for clean lists; dice lists will have tabs and digits)
- **No empty lines:** `grep -c '^$' lists/jalan-dewan-bahasa-{name}.txt` — expected: 0 for all files

**Part B: SQLite Database Verification (SQLI-01 through SQLI-08)**

For EACH of the 8 SQLite databases, verify:

```bash
sqlite3 db/{name}.db "SELECT COUNT(*) FROM words;"
sqlite3 db/{name}.db "PRAGMA integrity_check;"
```

| Database | Expected Rows |
|----------|--------------|
| `db/jalan-dewan-bahasa-besar.db` | 17,576 |
| `db/jalan-dewan-bahasa-sederhana.db` | 8,192 |
| `db/jalan-dewan-bahasa-dadu.db` | 7,776 |
| `db/jalan-dewan-bahasa-dadu-bersih.db` | 7,776 |
| `db/jalan-dewan-bahasa-kecil-alpha.db` | 1,296 |
| `db/jalan-dewan-bahasa-kecil-alpha-dadu.db` | 1,296 |
| `db/jalan-dewan-bahasa-kecil-qwerty.db` | 1,296 |
| `db/jalan-dewan-bahasa-kecil-qwerty-dadu.db` | 1,296 |

If `sqlite3` CLI is unavailable, use Python:
```python
import sqlite3
for db_name in [...eight db names...]:
    conn = sqlite3.connect(f'db/{db_name}')
    count = conn.execute('SELECT COUNT(*) FROM words').fetchone()[0]
    integrity = conn.execute('PRAGMA integrity_check').fetchone()[0]
    print(f'{db_name}: {count} rows, integrity={integrity}')
    conn.close()
```

**Part C: Record all results**

Create a verification summary table in stdout showing every file, its word count, UD status, entropy, and DB row count. Format:

```
=== VERIFICATION SUMMARY ===
File                          | Words   | UD    | Entropy | DB Rows  | DB OK
------------------------------|---------|-------|---------|----------|------
besar.txt                     | 17,576  | true  | 14.101  | 17,576   | ok
sederhana.txt                 | 8,192   | true  | 13.000  | 8,192    | ok
...
```

**Any failure = plan cannot complete.** Fix the issue or flag it as a blocker.
  </action>
  <verify>
    <automated>
      for f in lists/jalan-dewan-bahasa-*.txt; do
        name=$(basename "$f")
        tidy -AAAA -z nfc --locale ms "$f" 2>&1 | grep -q "Uniquely decodable? : true" || { echo "FAIL: $name not uniquely decodable"; exit 1; }
      done && echo "PASS: All .txt files uniquely decodable"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 .txt files pass `tidy -AAAA` with `Uniquely decodable? : true`
    - All 8 .txt files match target word counts exactly (see table above)
    - All 8 .txt files meet or exceed target entropy (see table above)
    - Dice format files have valid roll ranges (1111-6666 or 11111-66666)
    - Dadu bersih word set matches dadu word set identically
    - All 8 .db files pass `PRAGMA integrity_check` with result "ok"
    - All 8 .db files have correct row counts matching their .txt counterparts
  </acceptance_criteria>
  <done>All 8 .txt files and 8 .db files verified — every file meets its word count, UD, entropy, and integrity targets</done>
</task>

<task type="auto">
  <name>task 2: update README.md with final file listing — accurate word counts and entropy values for all lists</name>
  <files>README.md (modified)</files>
  <read_first>
    - README.md (current state — contains placeholder stats for long list section)
    - lists/jalan-dewan-bahasa-besar.txt (final long list — source of truth for stats)
  </read_first>
  <action>
Update the README.md "Senarai Besar Jalan Dewan Bahasa" section (currently lines 12-35) with actual statistics from the newly created `lists/jalan-dewan-bahasa-besar.txt`.

**Extract actual stats from the long list:**

```bash
tidy -AAAA -z nfc --locale ms lists/jalan-dewan-bahasa-besar.txt
```

Parse the `tidy -AAAA` output to get the exact values for:
- `List length` (should be 17576)
- `Mean word length` 
- `Shortest word` and its length
- `Longest word` and its length
- `Free of prefix words?` (expected: false)
- `Uniquely decodable?` (expected: true)
- `Entropy per word` (expected: 14.101 bits)
- `Efficiency per character`
- `Above brute force line?` (expected: true)
- `Mean edit distance`
- Sample example words (first 5-10 words from the file)

**Replace the placeholder stats block (lines 16-27 of README.md):**

The current block:
```text
Panjang senarai             : 17576 patah perkataan
Purata panjang perkataan    : 7.98 aksara
Panjang perkataan terpendek : 3 aksara (ada)
Panjang perkataan terpanjang: 15 aksara (penyelesaiannya)
Bebas perkataan awalan?     : false
Boleh dinyahkod secara unik?: true
Entropi per perkataan       : 14.101 bit
Kecekapan per aksara        : 1.767 bit
Atas garis daya kekerasan?  : true
Purata jarak suntingan      : 7.915
```

Replace with the ACTUAL values from `tidy -AAAA` output. Do NOT copy the placeholder values — use the real numbers. The format stays the same (one attribute per line, `Nama atribut : nilai`).

**Update example words (lines 28-35):**

Replace the example words block:
```text
Contoh perkataan
----------------
perjalanan kebersihan pengurusan masyarakat keupayaan
perkembangan pengalaman kemudahan peraturan keperluan
pembangunan pentadbiran keselamatan penyelidikan penubuhan
kemungkinan pembentukan perhubungan penyelesaian kedudukan
pengeluaran pelaksanaan kecekapan keseimbangan penerangan
```

With the FIRST 25 words (5 lines × 5 words) from `lists/jalan-dewan-bahasa-besar.txt`:
```bash
head -25 lists/jalan-dewan-bahasa-besar.txt | paste -d ' ' - - - - -
```
Format: 5 words per line, space-separated, same as existing README style. These words are alphabetically first in the list — if they look unnatural or are all prefixes, pick the first 25 sensible words (skip 1-2 letter words if needed for readability).

**Do NOT modify any other README sections** — the Sederhana, Dadu, Kecil, SQLite, and Licensing sections are already correct from Phase 1-3.

**Verify after editing:**
```bash
grep -A 25 "## Senarai Besar" README.md
```
Confirm the stats block matches `tidy -AAAA` output for `lists/jalan-dewan-bahasa-besar.txt`.
  </action>
  <verify>
    <automated>
      grep -q "Panjang senarai.*: 17576" README.md && \
      grep -q "Boleh dinyahkod secara unik?: true" README.md && \
      grep -q "Entropi per perkataan.*: 14.101" README.md && \
      echo "PASS: README long list section updated with actual stats"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md "Senarai Besar" section shows ACTUAL `tidy -AAAA` stats for `lists/jalan-dewan-bahasa-besar.txt`
    - Word count shows "17576 patah perkataan" (not any other number)
    - "Boleh dinyahkod secara unik?" shows "true"
    - Entropy shows "14.101 bit"
    - Example words come from the actual Malay long list file (not the English original)
    - No other README sections modified
  </acceptance_criteria>
  <done>README.md updated with accurate long list statistics and example words from jalan-dewan-bahasa-besar.txt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wordlist .txt files → tidy -AAAA | Verification reads files — tampered files would fail UD check |
| SQLite .db files → PRAGMA integrity_check | Database integrity verified programmatically |
| tidy output → README.md | Human-readable stats transcribed to documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | Verification bypass | mitigate | `tidy -AAAA` run on every file with grep for "Uniquely decodable? : true" — cannot fake this output |
| T-04-09 | Spoofing | README stats mismatch | mitigate | Stats extracted directly from `tidy -AAAA` output — grep verification confirms README values match expected targets |
| T-04-10 | Spoofing | SQLite row count mismatch | mitigate | `SELECT COUNT(*)` + `PRAGMA integrity_check` on every database — automated verification |
| T-04-11 | Information Disclosure | N/A — no PII | accept | Wordlists contain only common dictionary words |
| T-04-12 | Denial of Service | N/A — batch verification | accept | Verification is a one-time batch operation with no availability requirement |
</threat_model>

<verification>
**Automated verification script (runs all checks):**
```bash
# Verify all .txt files
for f in besar sederhana dadu dadu-bersih kecil-alpha kecil-alpha-dadu kecil-qwerty kecil-qwerty-dadu; do
  tidy -AAAA -z nfc --locale ms "lists/jalan-dewan-bahasa-${f}.txt" 2>&1 | \
    grep -E "List length|Uniquely decodable|Entropy per word|Above brute force"
done

# Verify all SQLite databases
for db in besar sederhana dadu dadu-bersih kecil-alpha kecil-alpha-dadu kecil-qwerty kecil-qwerty-dadu; do
  echo -n "db/jalan-dewan-bahasa-${db}.db: "
  sqlite3 "db/jalan-dewan-bahasa-${db}.db" "SELECT COUNT(*) FROM words; PRAGMA integrity_check;" 2>/dev/null | paste -s -d ' '
done

# Verify README stats
grep -A 25 "## Senarai Besar" README.md | grep -E "Panjang|Boleh dinyahkod|Entropi"
```
</verification>

<success_criteria>
- [ ] All 8 .txt files pass `tidy -AAAA` with `Uniquely decodable? : true` (XCUT-07)
- [ ] All 8 .txt files match exact word count targets
- [ ] All 8 .txt files meet or exceed entropy targets
- [ ] All 8 .db files pass `PRAGMA integrity_check` with result "ok"
- [ ] All 8 .db files have correct row counts
- [ ] `README.md` updated with accurate long list statistics from `tidy -AAAA`
- [ ] README.md example words come from actual `jalan-dewan-bahasa-besar.txt`
- [ ] Phase 4 success criteria 3-6 satisfied (ROADMAP.md)
</success_criteria>

<output>
After completion, create `.planning/phases/04-long-list-final-verification/04-02-SUMMARY.md`
</output>
